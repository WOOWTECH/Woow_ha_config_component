homeassistant:
  name: HA Config Editor Test
  unit_system: metric
  time_zone: Asia/Taipei

http:

frontend:

config_editor:

lovelace:
  mode: storage
  resources:
    - url: /local/config-editor-fullpage.js
      type: module

# test comment added by playwright